#!/bin/bash
set -euo pipefail
[[ $# -eq 0 ]] || exit 1
[[ -x /usr/local/bin/SkywarnPlus/SkyControl.py ]] || { echo "SkywarnPlus is not installed" >&2; exit 1; }

/usr/local/bin/SkywarnPlus/SkyControl.py enable true
